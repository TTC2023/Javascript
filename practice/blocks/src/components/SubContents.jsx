import React from 'react'

const SubContents = () => {
  const styles = {
    height: '50px',
    width: '50px',
    margin: '10px',
    backgroundColor: '#ffd966',
  };

  return (
    <div style={styles}>
    </div>
  );
}

export default SubContents