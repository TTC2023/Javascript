import React from 'react'

const Header = (props) => {
  const styles = {
    margin: '10px',
    height: '100px',
    width: '1000px',
    backgroundColor: '#6aa84f',
  };

  return (
    <div style={styles}>
    </div>
  );
}

export default Header